#include <stdio.h>

int main()
{
    int year;
    printf("enter the value year :");
    scanf("%d",&year);
    
    if(a%400==0)
    {
        printf("it is a leap year");
    }
    else if(year%100==0)
    {
        printf("it is not a leap year");
    }
    else if(year%4==0)
    {
        printf("it is not a leap year");
    }
    else
    {
        printf("it is a leap year");
    }
  
    return 0;
}






