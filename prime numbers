#include <stdio.h>

int main()
{
  int a;
  printf("enter the value:");
  scanf("%d", &a);
  if((a%2==1),(a==2))
  {
      printf("prime");
      
  }
 
else
{
    printf("not a prime");
}

 
    return 0;
}

