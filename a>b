#include <stdio.h>

int main()
{
    int a,b;
    printf("enter the value a :");
    scanf("%d",&a);
    printf("the enter the value of b:");
    scanf("%d",&b);
    if(a>b)
    {
        printf("a is max");
    }
    else
{
    printf("b is max");

}

    return 0;
}

