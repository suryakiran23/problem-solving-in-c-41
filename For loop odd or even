#include <stdio.h>

int main()
{
    int a;
    for(a=1;a<=21;a++)
    {
        if(a%2=0)
        {
            printf("\n even %d",a);
        }
        else
        {
            printf("\n odd %d",a);
        }
    }

    return 0;
}
